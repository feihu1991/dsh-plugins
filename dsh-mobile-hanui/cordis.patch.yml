# dsh-mobile-hanui bundle patch: host stub so client-modules can scan `dsh.client`
# (all mobile UI logic lives in the client-side src/client.js).
- insert:
    - id: dsh-mobile-hanui-shell
      name: dsh-mobile-hanui
